import { Category } from "@prisma/client";
import { SelectItem } from "../ui/select";

interface ItensProps {
  category: Category;
}

const Itens = ({ category }: ItensProps) => {
  return <></>;
};

export default Itens;
